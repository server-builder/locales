// The const name SHOULD match the file name.
const base = {
	name: "Translation Name", // This should be in the native language. For example, "Spanish" is "Español"
	emoji: "🍌", // This should be the flag of the native country using this language.
	authors: ["you!"], // (Markdown is supported!)
	keys: {
		// Put the keys here! any keys that aren't defined will fallback to en-US
	},
};

export default base; // Ensure that your const is defined!
